# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

### Класс ProductModel
Отвечает за хранение каталога товаров и данных о товаре, выбранном для подробного просмотра.

Конструктор:
`constructor()`

Поля класса:
`_items: IProduct[]` - массив всех товаров, доступных в каталоге.
`_previewItem: IProduct | null` - товар, выбранный для отображения в модальном окне предпросмотра.

Методы класса:
`setItems(items: IProduct[]): void` - сохраняет переданный в параметре массив товаров в поле _items.
`getItems(): IProduct[]` - возвращает массив всех товаров из поля _items.
`getItem(id: string): IProduct | null` - возвращает товар по его id, полученному в параметре. Если товар не найден, возвращает null.
`setPreviewItem(item: IProduct): void` - сохраняет переданный в параметре товар в поле _previewItem.
`getPreviewItem(): IProduct | null` - возвращает товар, сохраненный для предпросмотра.

### Класс CartModel
Управляет списком товаров, добавленных пользователем в корзину для покупки.

Конструктор:
`constructor()`

Поля класса:
`_items: IProduct[]` - массив товаров, находящихся в корзине.

Методы класса:
`getItems(): IProduct[]` - возвращает массив всех товаров в корзине.
`addItem(item: IProduct): void` - добавляет товар, полученный в параметре, в массив корзины.
`removeItem(item: IProduct): void` - удаляет товар, полученный в параметре, из массива корзины.
`clearCart(): void` - полностью очищает массив товаров в корзине.
`getTotalAmount(): number` - вычисляет и возвращает общую стоимость всех товаров в корзине.
`getItemsCount(): number` - возвращает общее количество товаров в корзине.
`checkItemInCart(id: string): boolean` - проверяет, находится ли товар с id, полученным в параметре, в корзине. Возвращает true, если товар присутствует, и false в противном случае.

### Класс BuyerModel
Хранит и валидирует данные покупателя, введенные при оформлении заказа.

Конструктор:
`constructor()`

Поля класса:
`_payment: TPayment` - способ оплаты.
`_email: string `- адрес электронной почты.
`_phone: string` - номер телефона.
`_address: string` - адрес доставки.

Методы класса:
`setData(data: Partial<IBuyer>): void` - сохраняет данные покупателя. Метод принимает объект типа Partial<IBuyer>, что позволяет обновлять как все поля сразу, так и одно или несколько, не затрагивая остальные.
`getData(): IBuyer` - возвращает объект со всеми текущими данными покупателя.
`clearData(): void` - сбрасывает все данные покупателя к значениям по умолчанию.
`validateData():` IBuyerValidationResult - выполняет валидацию данных. Возвращает объект типа IBuyerValidationResult, в котором ключи — это названия полей (payment, email, phone, address), а значения — строки с текстом ошибки. Если поле прошло валидацию, соответствующего ключа в объекте не будет.

Пример возвращаемого объекта при валидации:
{
  payment: 'Не выбран вид оплаты',
  email: 'Укажите емэйл',
}
Поля `phone` и `address` в данном случае валидны.

### Данные
Для описания структур данных, используемых в приложении, разработаны следующие интерфейсы:

Интерфейс товара (IProduct):
Описывает структуру данных отдельного товара в каталоге.

interface IProduct {
  id: string; // Уникальный идентификатор товара
  description: string; // Подробное описание товара
  image: string; // Ссылка на изображение товара
  title: string; // Название товара
  category: string; // Категория, к которой принадлежит товар
  price: number | null; // Цена товара. Может быть null, если цена не установлена.
}

Интерфейс покупателя (IBuyer):
Описывает контактные и платежные данные покупателя, необходимые для оформления заказа.

interface IBuyer {
  payment: TPayment; // Выбранный способ оплаты
  email: string; // Адрес электронной почты
  phone: string; // Номер телефона
  address: string; // Адрес доставки
}

### Слой коммуникации 
Обеспечивает коммуникацию с сервером магазина. Отвечает за получение каталога товаров и отправку данных заказа.

Конструктор:
`constructor(baseApi: IApi)` - принимает объект, соответствующий интерфейсу IApi, для выполнения HTTP-запросов.

Поля класса:
`baseApi: IApi` - базовый объект для выполнения API-запросов.

Методы класса:
`getProductList(): Promise<IProduct[]>` - выполняет GET запрос на эндпоинт /product/ и возвращает промис с массивом товаров.

`submitOrder(orderData: IOrderData): Promise<object>` - выполняет POST запрос на эндпоинт /order/ с данными заказа, полученными в параметрах метода, и возвращает промис с ответом сервера.

## Документация классов слоя Представления (View)
Общие принципы:
Каждый класс представления отвечает за свой блок разметки

Все компоненты наследуются от базового класса Component

В ответ на любое действие пользователя генерируется событие для обработки Презентером

## Классы карточек товаров (с общим родителем)
Базовый класс Card
Базовый класс для всех видов карточек товаров. Выносит общий функционал для отображения товаров.

## Конструктор:
`constructor(container: HTMLElement)` - принимает контейнер для отображения карточки.

Поля класса:

`_id: string` - идентификатор товара

`_title: string` - название товара

`_price: number | null` - цена товара

`_category: string` - категория товара

`_image: string` - URL изображения товара

Методы класса:

`set id(value: string): void` - сеттер для идентификатора товара

`set title(value: string): void` - сеттер для названия товара

`set price(value: number | null): void` - сеттер для цены товара

`set category(value: string): void` - сеттер для категории товара

`set image(value: string): void` - сеттер для изображения товара

`set categoryColor(value: string): void` - сеттер для цвета категории

Класс CatalogItem
Отвечает за отображение карточки товара в основном каталоге. Использует шаблон #card-catalog.

Конструктор:
`constructor(container: HTMLElement)` - наследует от Card.

Методы класса:

`render(): HTMLElement` - отрисовывает карточку товара в каталоге

Генерирует событие: 'catalog:add-to-cart' при клике на карточку

Класс PreviewItem
Отвечает за отображение карточки товара в модальном окне предпросмотра. Использует шаблон #card-preview.

Конструктор:
`constructor(container: HTMLElement)` - наследует от Card.

Методы класса:

`render(): HTMLElement` - отрисовывает подробную карточку товара

`set description(value: string): void` - сеттер для описания товара

Генерирует событие: 'preview:add-to-cart' при клике на кнопку "В корзину"

Класс BasketItem
Отвечает за отображение карточки товара в корзине. Использует шаблон #card-basket.

Конструктор:
`constructor(container: HTMLElement)` - наследует от Card.

Поля класса:

`_index: number` - порядковый номер товара в корзине

Методы класса:

`render(): HTMLElement` - отрисовывает карточку товара в корзине

`set index(value: number): void` - сеттер для порядкового номера

Генерирует событие: 'basket:remove-item' при клике на кнопку удаления

Классы форм (с общим родителем)
Базовый класс Form
Абстрактный базовый класс для всех форм. Выносит общий функционал валидации и обработки.

Конструктор:
`constructor(container: HTMLElement)` - принимает контейнер для отображения формы.

Методы класса:

`render(): HTMLElement` - отрисовывает форму

`validate(): boolean` - выполняет базовую валидацию формы

`clear(): void` - очищает поля формы

`set errors(value: string[]): void` - отображает ошибки валидации

Класс OrderForm
Назначение и зона ответственности:
Отвечает за форму выбора способа оплаты и ввода адреса доставки. Использует шаблон #order.

Конструктор:
`constructor(container: HTMLElement)` - наследует от Form.

Поля класса:

`_payment: string` - выбранный способ оплаты

`_address: string` - введенный адрес доставки

Методы класса:

`render(): HTMLElement` - отрисовывает форму заказа

`set payment(value: string): void` - сеттер для способа оплаты

`set address(value: string): void` - сеттер для адреса доставки

`validate(): boolean` - валидирует форму заказа

Генерирует события:

'order:payment-changed' при изменении способа оплаты

'order:address-changed' при изменении адреса

'order:submit' при отправке формы

Класс ContactsForm
Отвечает за форму ввода контактных данных покупателя. Использует шаблон #contacts.

Конструктор:
`constructor(container: HTMLElement)` - наследует от Form.

Поля класса:

`_email: string` - введенный email

`_phone: string` - введенный телефон

Методы класса:

`render(): HTMLElement` - отрисовывает форму контактов

`set email(value: string): void` - сеттер для email

`set phone(value: string): void` - сеттер для телефона

`validate(): boolean` - валидирует форму контактов

Генерирует события:

'contacts:email-changed' при изменении email

'contacts:phone-changed' при изменении телефона

'contacts:submit' при отправке формы

Класс модального окна
Класс Modal
Отвечает за управление модальными окнами. Не имеет дочерних классов.

Конструктор:
`constructor(container: HTMLElement)` - принимает контейнер модального окна.

Поля класса:

`_content: HTMLElement | null` - текущий контент модального окна

`_isOpen: boolean` - флаг открытого состояния

Методы класса:

`open(): void` - открывает модальное окно

`close(): void` - закрывает модальное окно

`set content(value: HTMLElement): void` - устанавливает контент в модальное окно

`clear(): void` - очищает контент модального окна

Генерирует события:

'modal:open' при открытии модального окна

'modal:close' при закрытии модального окна

Другие компоненты View
Класс Gallery
Отвечает за отображение каталога товаров. Контейнер для карточек CatalogItem.

Конструктор:
`constructor(container: HTMLElement)` - принимает контейнер галереи.

Методы класса:

`render(items: IProduct[]): HTMLElement` - отрисовывает каталог товаров

`clear(): void` - очищает каталог

Класс Basket
Отвечает за отображение содержимого корзины. Использует шаблон #basket.

Конструктор:
`constructor(container: HTMLElement)` - принимает контейнер корзины.

Поля класса:

`_items: BasketItem[]` - массив товаров в корзине

`_total: number` - общая сумма корзины

Методы класса:

`render(items: IProduct[]): HTMLElement` - отрисовывает корзину с товарами

`set total(value: number): void` - сеттер для общей суммы

Генерирует событие: 'basket:open-order' при клике на кнопку "Оформить"

Класс Header
Отвечает за отображение шапки сайта с логотипом и счетчиком корзины.

Конструктор:
`constructor(container: HTMLElement)` - принимает контейнер шапки.

Методы класса:

`render(): HTMLElement` - отрисовывает шапку

`set counter(value: number): void` - обновляет счетчик товаров в корзине

Генерирует событие: 'header:open-basket' при клике на иконку корзины

Класс Success
Отвечает за отображение успешного оформления заказа. Использует шаблон #success.

Конструктор:
`constructor(container: HTMLElement)` - принимает контейнер для отображения.

Методы класса:

`render(total: number): HTMLElement` - отрисовывает сообщение об успешном заказе

Генерирует событие: 'success:close' при клике на кнопку закрытия

## События приложения:
### Карточки товаров
`catalog:add-to-cart` - генерация при клике на карточку товара в каталоге. Содержит данные о товаре.

`preview:add-to-cart` - генерация при клике на кнопку "В корзину" в модальном окне предпросмотра. Содержит данные о товаре.

`basket:remove-item` - генерация при клике на кнопку удаления товара из корзины. Содержит данные о товаре и его индексе.

### Модели данных
`products:changed` - генерация при изменении каталога товаров. Содержит массив товаров.

`preview:changed` - генерация при изменении товара для предпросмотра. Содержит товар.

`cart:changed` - генерация при любом изменении корзины. Содержит массив товаров, действие и измененный товар.

`buyer:changed` - генерация при изменении данных покупателя. Содержит все данные покупателя.

`buyer:payment-changed` - генерация при изменении способа оплаты.

`buyer:email-changed` - генерация при изменении email.

`buyer:phone-changed` - генерация при изменении телефона.

`buyer:address-changed` - генерация при изменении адреса.

`buyer:cleared` - генерация при очистке данных покупателя.

### Формы:
`order:payment-changed` - генерация при изменении способа оплаты. Содержит выбранный способ оплаты.

`order:address-changed` - генерация при изменении адреса доставки. Содержит введенный адрес.

`order:submit` - генерация при отправке формы заказа. Содержит данные формы.

`contacts:email-changed` - генерация при изменении email. Содержит введенный email.

`contacts:phone-changed` - генерация при изменении телефона. Содержит введенный телефон.

`contacts:submit` - генерация при отправке формы контактов. Содержит данные формы.

### Модальные окна и интерфейс:
`modal:open` - генерация при открытии модального окна.

`modal:close` - генерация при закрытии модального окна.

`header:open-basket` - генерация при клике на иконку корзины в шапке.

`basket:open-order` - генерация при клике на кнопку "Оформить" в корзине.

`success:close` - генерация при закрытии окна успешного оформления заказа.

